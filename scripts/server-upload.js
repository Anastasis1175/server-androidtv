async function uploadFile() { const fileInput = document.getElementById('fileInput'); // Check if the user actually selected a file if (fileInput.files.length === 0) { alert('Please select a file first!'); return; } const file = fileInput.files[0]; // Create a FormData object to hold the file data const formData = new FormData(); formData.append('uploadedFile', file); try { // Send the file to your server endpoint const response = await fetch('/upload', { method: 'POST', body: formData // fetch automatically sets the correct Content-Type header }); if (response.ok) { const result = await response.json(); alert('File uploaded successfully: ' + result.message); } else { alert('Upload failed with status: ' + response.status); } } catch (error) { console.error('Error uploading file:', error); alert('An error occurred during the upload.'); } }
